/* Make UpperCase */

/*
Write a function which converts the input string to uppercase.
*/

function makeUpperCase(str) {
    return str.toUpperCase();
}

// Alternate solution: 
// const makeUpperCase = str => str.toUpperCase();

// Test: 
// word = "hello"
// console.log(makeUpperCase(word))